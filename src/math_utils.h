#ifndef MATH_UTILS_H
#define MATH_UTILS_H

namespace math_utils {
    int add(int a, int b);
    int multiply(int a, int b);
    double divide(int a, int b); // Якщо b == 0 – буде викинуто виняток
}

#endif // MATH_UTILS_H
